import { Grid } from "@material-ui/core";
import React from "react";

const About = () => {
  return (
    <Grid
      container
      item
      style={{ justifyContent: "center", marginTop: "100px" }}
    >
      <Grid item>This is About Page</Grid>
    </Grid>
  );
};

export default About;
